# Database (READ-ONLY — same DB as beat-books-data)
DATABASE_URL=postgresql://user:password@host:5432/beatthebooks

# Model artifacts
MODEL_ARTIFACTS_PATH=model_artifacts

# Feature engineering
DEFAULT_ROLLING_WINDOWS=3,5,10

# App
LOG_LEVEL=INFO
